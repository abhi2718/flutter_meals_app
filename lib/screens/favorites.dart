import 'package:flutter/widgets.dart';

class FavoritesMeals extends StatelessWidget {
  const FavoritesMeals({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child:const Text('mark as favorite')
    );
  }
}